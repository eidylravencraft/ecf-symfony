{% block navbar %}
  <nav class="navbar navbar-expand-lg bg-body-tertiary bg-custom">
    <div class="container-fluid">
      <a class="navbar-brand text-dark fw-bold" href="#">ADMINISTRATION</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active text-dark fw-bold" aria-current="page" href="{{ path('app_admin_user_index') }}">{{ 'navbar.users'|trans }}</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-dark fw-bold" href="#">{{ 'navbar.books'|trans }}</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-dark fw-bold" href="{{ path('app_admin_workplace_index') }}">{{ 'navbar.workplaces'|trans }}</a>
          </li>
          <li class="nav-item">
            <a class="nav-link fw-bold" href="{{ path('app_home') }}" style="color: lightgreen;">{{ 'navbar.return'|trans }}</a>
          </li>
        </ul>
        {% if is_granted('ROLE_USER') or is_granted('ROLE_ADMIN') or is_granted('ROLE_SUBSCRIBER') %}
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle text-dark fw-bold" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Bienvenue {{ app.user.firstName }}</a>
              <ul class="dropdown-menu">
                <li>
                  <a class="dropdown-item" href="{{ path('profile_user') }}">Mon compte</a>
                </li>
                <li>
                  <a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
              </ul>
            </li>
          </ul>
          {# {% elseif  is_granted("ROLE_ADMIN") %}
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
             <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Bienvenue {{ app.user.firstName }}
              </a>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Admin</a></li>
                <li><a class="dropdown-item" href="{{ path('profile_user') }}">Mon compte</a></li>
                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a></li>
              </ul>
            </li>
         </ul> #}
        {% else %}
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="{{ path('app_login') }}">Se connecter</a>
            </li>
          </ul>
        {% endif %}
      </div>
    </div>
  </nav>
{% endblock %}
